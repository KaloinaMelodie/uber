<%= form_with(model: idchemin) do |form| %>
  <% if idchemin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(idchemin.errors.count, "error") %> prohibited this idchemin from being saved:</h2>

      <ul>
        <% idchemin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :idchem, style: "display: block" %>
    <%= form.text_field :idchem %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
